import React, {Component} from 'react';

import HomeContainer from '../containers/Home/HomeContainer';

export default ({}) => {
    return (
        <div>
            <HomeContainer />
        </div>
    );
}