import React, {Component} from 'react';

import SampleContainer from '../containers/Sample/SampleContainer';

export default ({}) => {
    return (
        <div>
            <SampleContainer />
        </div>
    );
}